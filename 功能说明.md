# Blackout 功能说明

## 1. 软件定位

Blackout 是一个 Windows 托盘小工具，用来快速让屏幕变黑（用纯黑全屏窗口遮挡屏幕并把亮度降低到0），便于临时遮挡屏幕内容。

## 2. 基本使用方式

- 软件启动后常驻系统托盘。
- 用户通过托盘图标完成所有操作。

## 3. 需要具备的功能

### 3.1 黑屏模式

软件需要支持三种黑屏范围：

1. 全部屏幕黑屏
2. 仅主屏黑屏
3. 仅副屏黑屏

### 3.2 快速开关

- 左键点击托盘图标可以快速开/关黑屏。
- 黑屏状态下再次触发应恢复正常显示。

### 3.3 托盘菜单

右键菜单至少包含：

1. 一键黑屏
2. 主屏黑屏
3. 副屏黑屏
4. 重启软件
5. 退出软件

### 3.4 黑屏退出方式

黑屏时，至少支持以下退出方式：

- 按 Esc
- 鼠标点击黑屏区域
- 再次点击托盘图标

### 3.5 多显示器支持

- 能识别主屏和副屏。
- 在多显示器场景下，黑屏范围应与所选模式一致。
- 最好能识别所有显示器实现单独控制每一个屏幕的黑屏（能做到就做）

### 3.6 亮度体验

- 进入黑屏时，屏幕亮度应降低到0。
- 退出黑屏时，亮度恢复到黑屏前状态。

## 4. 结果要求

- 黑屏进入快、退出快。
- 黑屏和恢复过程稳定，不影响继续使用电脑。
- 重启与退出功能可正常使用。

## 5. 核心可靠性与全屏技术机制（v1.2.1）

- **管理员自启 XML 级自愈**：保持 `requireAdministrator` 最高压制权限（确保压住任务管理器等高权限置顶窗口且任意焦点下 `Esc` 100% 生效），通过完整 Task Scheduler 1.2 XML 注册 `Blackout_AutoStart`（关闭 `DisallowStartIfOnBatteries`、`StopIfGoingOnBatteries` 和 72 小时超时限制，开启 `RestartOnFailure` 崩溃自动重启），启动时默认自动创建/升级。
- **托盘图标不死鸟机制**：启动前等待 `Shell_TrayWnd` 就绪 + 30 秒健康守护；监听 `TaskbarCreated` 自动重挂；重复运行自动唤醒/接管旧实例；释放单实例 `Mutex` 后安全重启。
- **Win32 原生右上方托盘菜单**：采用 `CreatePopupMenu` + `TrackPopupMenuEx` + `uxtheme` 深浅色自适应，弹出前屏蔽系统 `MenuDropAlignment` 左偏默认设置，100% 在鼠标右上方展开，并支持通过“指定屏幕 ▸”单独控制任意一块显示器的黑屏与 0 亮度。
- **物理 `HMONITOR` 屏幕隔离与防闪烁**：采用 `MonitorFromWindow` 核对物理句柄，仅隐藏已黑屏屏幕上的任务栏与系统通知（`<1ms` 零帧隐藏），未黑屏副屏上的 DisplayFusion 任务栏与 QQ 截图（`TXGuiFoundation`）100% 隔离不闪烁。
- **静态常驻委托与亮度保护**：所有 Win32 P/Invoke 回调委托声明为 `static readonly` 防止 `.NET GC` 回收引发 `0xc0000005` 闪退；按 `HMONITOR` 保护非零原始亮度；黑屏窗口采用 `PerMonitorV2` 物理像素贴合与窗口级 32×32 透明光标。

